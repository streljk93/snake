'use strict';

// main config of application
snake.config = snake.config || {

    application: {
        speed: 160,
    },

    board: {
        size: 500,
        row: 25,
        cell: 25,
    },

    square: {
        size: 20,
    },

};
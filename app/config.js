'use strict';

// main config of application
snake.config = snake.config || {

    application: {
        speed: 1000,
    },

    board: {
        size: 500,
        row: 10,
        cell: 10,
    },

    square: {
        size: 50,
    },

};
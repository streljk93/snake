'use strict';

// component Board
snake.components = snake.components || {};
snake.components.Board = snake.components.Board || (function () {

    /**
     * Change position of snake on the board to left side
     * @param {number} x
     * @param {number} y
     */
    function changeSnakePosition(x, y) {

    }

    return {
        changeSnakePosition: changeSnakePosition,
    };

})();